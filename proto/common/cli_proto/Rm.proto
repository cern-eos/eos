syntax="proto3";
package eos.console;

message RmProto {
  message IdentifierProto {
    oneof Identifier {
      uint64 Fid = 1;
      uint64 Cid = 2;
      string Path = 3;
    }
  }

  bool Recursive = 4;
  bool BypassRecycle = 5;
  repeated IdentifierProto identifier = 6;
}
