--- a/src/http/ngx_http_upstream.c	2013-07-17 14:51:22.000000000 +0200
+++ b/src/http/ngx_http_upstream.c	2014-11-11 00:33:57.000000000 +0100
@@ -518,8 +518,10 @@
         r->write_event_handler = ngx_http_upstream_wr_check_broken_connection;
     }
 
-    if (r->request_body) {
-        u->request_bufs = r->request_body->bufs;
+    if (r->method != NGX_HTTP_PUT) {
+	if (r->request_body) {
+            u->request_bufs = r->request_body->bufs;
+        }
     }
 
     if (u->create_request(r) != NGX_OK) {
