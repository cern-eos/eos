# ------------------------------------------------------------ #
[ha:init]
# ------------------------------------------------------------ #
set -x

eos root://127.0.0.1 route link /eos/ ${SERVER_HOST}:1090:8444,${SERVER_HOST}:1091:8445,${SERVER_HOST}:1092:8446
eos root://127.0.0.1 route ls

# ------------------------------------------------------------ #
[fst:init]
# ------------------------------------------------------------ #

[fst:sysconfig]
